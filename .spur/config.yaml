# Spur project configuration for dream-vault
# Generated by `spur init`

project:
  name: dream-vault

defaults:
  agent: pi

rules:
  enabled: true
  paths:
    - .spur/rules/**/*.yaml

workflows:
  paths:
    - .spur/workflows/

redaction:
  enabled: false

storage:
  dataDir: data
