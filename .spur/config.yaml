# Spur project configuration for dream-vault
# Generated by `spur init`
version: "0.1"
name: dream-vault
description: Obsidian vault template with AI-augmented content creation
defaults:
  agent: pi

rules:
  allowExtensions: false
  enabled: true
  paths:
    - .spur/rules/**/*.yaml

workflows:
  paths:
    - .spur/workflows/

redaction:
  enabled: false

storage:
  dataDir: data
